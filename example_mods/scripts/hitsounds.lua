function onUpdate(elapsed)
        if getPropertyFromClass('flixel.FlxG', 'keys.justPressed.H') then

function goodNoteHit(id, noteData, noteType, isSustainNote)
playSound('ChartingTick', 0.34);

end
    end
        end